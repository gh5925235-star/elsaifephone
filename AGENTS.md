# Project Guidelines

- This is a TanStack Start v1 project.
- Keep all routes under `src/routes`.
- Use Tailwind CSS v4 semantic tokens; avoid hardcoded colors.
- Store business logic in `src/lib`.
- Prefer `createServerFn` for backend operations.
